const should = require('chai').should;

describe('Node/Express server', function(){
  it('should return index.html from root route');
  it('should add new movies to db');
  it('should delete movies from db');
  it('should return all movies from db');
  
})

